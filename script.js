// Этот скрипт очень простой, так как весь контент в HTML.
// Вы можете использовать его для добавления интерактивности позже.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Блог загружен! Весь контент находится в index.html.');

    // Пример: Если вы хотите, чтобы кнопка "Читать далее" что-то делала
    // const readMoreButtons = document.querySelectorAll('.read-more');
    // readMoreButtons.forEach(button => {
    //     button.addEventListener('click', (event) => {
    //         event.preventDefault();
    //         alert('Это просто заглушка. В реальном блоге здесь был бы переход на полную статью.');
    //     });
    // });
});
